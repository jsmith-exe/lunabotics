"""
A file we can dump other things into if we want to start up a process in an emergency
"""

from launch import LaunchDescription

def generate_launch_description():
    print("Blank")

    return LaunchDescription([

    ])
